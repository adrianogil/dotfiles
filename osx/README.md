# dotfiles for OSX

# Folder structure

- terminal_style: stores few styles for MacOS terminal
                  styles can be update by running the command:
```
cd terminal_style
./download_styles.sh
```